name: youbike_android
description: YouBike Android App Translation

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  http: ^1.1.0
  geolocator: ^10.1.0
  flutter_map_marker_cluster: ^1.3.0 # 新增：解決圖釘聚合問題

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
